import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Inventory',
};

const Page = () => {
    return (
        <>
            <section>ok</section>
        </>
    );
};

export default Page;
